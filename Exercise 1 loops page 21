#include<iostream>
using namespace std;
int main()
{
	int i, n, a, sum=0;
	cout << "Enter the number of terms ";
	cin >> n;
	
	for (i = 1; i >0; i++)

	{

		cout << "Enter term " << i << endl;
		cin >> a;
		sum += a;
		if (i >= n) { break; }
		


	}
	cout << "Sum is " << sum << endl;
}
